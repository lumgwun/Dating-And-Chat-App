<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item_root_chat"
    style="@style/ListItem"
    tools:ignore="RtlHardcoded">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/chat_user"
            style="@style/UserIconStyle"
            tools:background="@color/color_new_blue"/>

        <TextView
            android:id="@+id/chat_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:paddingBottom="2dp"
            android:textColor="@color/text_color_white"
            android:textSize="17sp"
            android:textStyle="normal"
            tools:text="E" />
    </RelativeLayout>

    <TextView
        android:id="@+id/chat_user_login"
        style="@style/UserTitleStyle"
        tools:text="abrakadabra"/>

    <CheckBox
        android:id="@+id/chat_checkbox"
        style="@style/UserCheckboxStyle" />
</LinearLayout>